# GroomPro environment variables
# GoDaddy production values belong in the hosting environment, never in GitHub.
DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/DATABASE"

# Used later for secure first-run/bootstrap configuration.
GROOMPRO_SETUP_SECRET="replace-with-a-long-random-secret"

# Optional integrations added in later phases.
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_PHONE_NUMBER=""
