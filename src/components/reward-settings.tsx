"use client";
import {useEffect,useState} from "react";
export function RewardSettings(){const[fee,setFee]=useState("45");const[rate,setRate]=useState("1");const[msg,setMsg]=useState("");const[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/reward-settings").then(r=>r.json()).then(d=>{if(d.noShowFeeCents!=null)setFee(String(Number(d.noShowFeeCents)/100);if(d.rewardPointsPerDollar!=null)setRate(String(d.rewardPointsPerDollar));}).finally(()=>setLoading(false))},[]);async function save(){const feeCents=Math.round(Number(fee)*100);const pointsPerDollar=Number(rate);if(!Number.isFinite(feeCents)||feeCents<=0||!Number.isFinite(pointsPerDollar)||pointsPerDollar<0){setMsg("Enter a valid No-Show Fee and Reward Points rate.");return;}const r=await fetch("/api/reward-settings",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({noShowFeeCents:feeCents,rewardPointsPerDollar:pointsPerDollar})});const d=await r.json();setMsg(r.ok?"Customer account and reward settings saved.":(d.error||"Could not save settings."))}return <div className="gp-settings-card"><h2>Customer Account & Secret Rewards</h2><p>Managers can control the No-Show Fee and how many Reward Points are earned per dollar spent.</p><div style={{display:"grid",gridTemplateColumns:"1fr 1fr auto",gap:10,alignItems:"end"}}><div><span className="gp-label">No-Show Fee</span><input className="gp-search-input" inputMode="decimal" value={fee} onChange={e=>setFee(e.target.value)} disabled={loading}/></div><div><span className="gp-label">Reward Points per $1 Spent</span><input className="gp-search-input" inputMode="decimal" value={rate} onChange={e=>setRate(e.target.value)} disabled={loading}/></div><button className="gp-btn primary" onClick={save} disabled={loading}>Save</button></div>{msg&&<div className="gp-form-success" style={{marginTop:10}}>{msg}</div>}</div>}
