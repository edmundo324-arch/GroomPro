import { NextRequest, NextResponse } from "next/server";
import { db } from "@/src/lib/db";
import { hashPin } from "@/src/lib/pin";
function tenantIdFrom(request: NextRequest) { return request.headers.get("x-tenant-id") || request.cookies.get("groompro_tenant")?.value || process.env.GROOMPRO_DEV_TENANT_ID || null; }
export async function GET(request: NextRequest) { const tenantId=tenantIdFrom(request); if(!tenantId)return NextResponse.json({error:"Tenant context is required."},{status:401}); const employees=await db.user.findMany({where:{tenantId},select:{id:true,firstName:true,lastName:true,role:true,active:true,locationId:true},orderBy:[{active:"desc"},{lastName:"asc"},{firstName:"asc"}]}); return NextResponse.json({employees}); }
export async function POST(request: NextRequest) { const tenantId=tenantIdFrom(request); if(!tenantId)return NextResponse.json({error:"Tenant context is required."},{status:401}); const body=await request.json(); const firstName=String(body.firstName||"").trim(),lastName=String(body.lastName||"").trim(),role=String(body.role||"FRONT"),pin=String(body.pin||""); if(!firstName||!lastName)return NextResponse.json({error:"First and last name are required."},{status:400}); if(!/^\d{4,10}$/.test(pin))return NextResponse.json({error:"PIN must contain 4–10 digits."},{status:400}); const existing=await db.user.findFirst({where:{tenantId,firstName,lastName}}); if(existing)return NextResponse.json({error:"An employee with that name already exists."},{status:409}); const user=await db.user.create({data:{tenantId,locationId:body.locationId?String(body.locationId):null,firstName,lastName,role:role as any,pinHash:await hashPin(pin),active:true}}); return NextResponse.json({employee:{id:user.id,firstName:user.firstName,lastName:user.lastName,role:user.role,active:user.active,locationId:user.locationId}},{status:201}); }
export async function PATCH(request: NextRequest) { const tenantId=tenantIdFrom(request); if(!tenantId)return NextResponse.json({error:"Tenant context is required."},{status:401}); const body=await request.json(),id=String(body.id||""); if(!id)return NextResponse.json({error:"Employee id is required."},{status:400}); const existing=await db.user.findFirst({where:{id,tenantId}}); if(!existing)return NextResponse.json({error:"Employee not found."},{status:404}); const data:any={}; if(body.firstName!==undefined)data.firstName=String(body.firstName).trim(); if(body.lastName!==undefined)data.lastName=String(body.lastName).trim(); if(body.role!==undefined)data.role=String(body.role) as any; if(body.locationId!==undefined)data.locationId=body.locationId?String(body.locationId):null; if(body.active!==undefined)data.active=Boolean(body.active); if(body.pin!==undefined&&String(body.pin)!==""){if(!/^\d{4,10}$/.test(String(body.pin)))return NextResponse.json({error:"PIN must contain 4–10 digits."},{status:400});data.pinHash=await hashPin(String(body.pin));} const user=await db.user.update({where:{id},data}); return NextResponse.json({employee:{id:user.id,firstName:user.firstName,lastName:user.lastName,role:user.role,active:user.active,locationId:user.locationId}}); }
