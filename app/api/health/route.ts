import {NextRequest,NextResponse} from "next/server";
import {db} from "@/src/lib/db";
export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const started=Date.now();const tenantId=request.headers.get("x-tenant-id")||request.cookies.get("groompro_tenant")?.value||process.env.GROOMPRO_DEV_TENANT_ID||null;try{await db.$queryRaw`SELECT 1`;const tenant=tenantId?await db.tenant.findUnique({where:{id:tenantId},select:{id:true,name:true}}):await db.tenant.findFirst({orderBy:{createdAt:"asc"},select:{id:true,name:true}});const t=tenant?.id||null;const [locations,employees,customers,pets,tickets]=t?await Promise.all([db.location.count({where:{tenantId:t}}),db.user.count({where:{tenantId:t}}),db.customer.count({where:{tenantId:t}}),db.pet.count({where:{tenantId:t}}),db.ticket.count({where:{tenantId:t}})]):[0,0,0,0,0];return NextResponse.json({ok:true,database:"connected",tenant,counts:{locations,employees,customers,pets,tickets},responseMs:Date.now()-started},{headers:{"Cache-Control":"no-store"}})}catch(error){console.error("Health check failed",error);return NextResponse.json({ok:false,database:"unavailable",error:"Database health check failed.",responseMs:Date.now()-started},{status:503,headers:{"Cache-Control":"no-store"}})}}
