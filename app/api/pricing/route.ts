import { NextRequest,NextResponse } from "next/server";import { Prisma } from "@prisma/client";import { db } from "@/src/lib/db";import { bulkIncreaseBasePrices,getPackagePrice,getServicePrice } from "@/src/lib/pricing";import { getActiveEmployeeSession } from "@/src/lib/employee-session";
const COOKIE="groompro_session";const tenantFrom=(r:NextRequest)=>r.headers.get("x-tenant-id")||process.env.GROOMPRO_DEV_TENANT_ID||"";async function actor(r:NextRequest){const t=tenantFrom(r),sid=r.cookies.get(COOKIE)?.value||"";return t&&sid?getActiveEmployeeSession(t,sid):null}
export async function GET(r:NextRequest){const tenantId=tenantFrom(r);if(!tenantId)return NextResponse.json({error:"Tenant context is required."},{status:401});const locationId=r.nextUrl.searchParams.get("locationId"),type=r.nextUrl.searchParams.get("type")||"SERVICE",entityId=r.nextUrl.searchParams.get("entityId");if(locationId&&entityId){const price=type==="PACKAGE"?await getPackagePrice(tenantId,locationId,entityId):await getServicePrice(tenantId,locationId,entityId);return price?NextResponse.json({type,entityId,locationId,price}):NextResponse.json({error:"Priceable item not found."},{status:404})}if(type==="PACKAGE"){const rows=await db.$queryRaw<Array<{id:string;name:string;basePriceCents:number;priceAdjustmentPct:Prisma.Decimal|number|null;priceOverrideCents:number|null}>>(Prisma.sql`SELECT p.id,p.name,p.basePriceCents,COALESCE(lpp.priceAdjustmentPct,0) AS priceAdjustmentPct,lpp.priceOverrideCents FROM Package p LEFT JOIN LocationPackagePricing lpp ON lpp.packageId=p.id AND lpp.locationId=${locationId??""} AND lpp.tenantId=${tenantId} AND lpp.active=true WHERE p.tenantId=${tenantId} AND p.active=true ORDER BY p.name ASC`);return NextResponse.json({packages:rows.map(row=>({...row,adjustmentPct:Number(row.priceAdjustmentPct??0),finalPriceCents:row.priceOverrideCents!=null?row.priceOverrideCents:Math.round(row.basePriceCents*(1+Number(row.priceAdjustmentPct??0)/100))}))})}const rows=await db.$queryRaw<Array<{id:string;name:string;category:string|null;basePriceCents:number;priceAdjustmentPct:Prisma.Decimal|number|null;priceOverrideCents:number|null}>>(Prisma.sql`SELECT s.id,s.name,s.category,s.priceCents AS basePriceCents,COALESCE(lsp.priceAdjustmentPct,0) AS priceAdjustmentPct,lsp.priceOverrideCents FROM Service s LEFT JOIN LocationServicePricing lsp ON lsp.serviceId=s.id AND lsp.locationId=${locationId??""} AND lsp.tenantId=${tenantId} AND lsp.active=true WHERE s.tenantId=${tenantId} AND s.active=true ORDER BY s.category ASC,s.name ASC`);return NextResponse.json({services:rows.map(row=>({...row,adjustmentPct:Number(row.priceAdjustmentPct??0),finalPriceCents:row.priceOverrideCents!=null?row.priceOverrideCents:Math.round(row.basePriceCents*(1+Number(row.priceAdjustmentPct??0)/100))}))})}
export async function POST(r:NextRequest){const tenantId=tenantFrom(r),session=await actor(r);if(!tenantId||!session)return NextResponse.json({error:"Employee PIN is required for bulk pricing changes."},{status:401});if(session.user.role!=="ADMIN"&&session.user.role!=="MANAGER")return NextResponse.json({error:"Manager access required."},{status:403});try{const b=await r.json();const result=await bulkIncreaseBasePrices(tenantId,{entityType:b.entityType==="PACKAGE"?"PACKAGE":"SERVICE",adjustmentPct:Number(b.adjustmentPct),entityIds:Array.isArray(b.entityIds)?b.entityIds:undefined,reason:typeof b.reason==="string"?b.reason:undefined,actorUserId:session.user.id});return NextResponse.json(result)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to update pricing."},{status:400})}}
