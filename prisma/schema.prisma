generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Existing schema retained; checkout totals are stored on Ticket so the transaction can preserve the amount charged.
// Full schema is maintained in the repository; this change adds the following Ticket fields:
// tipCents Int @default(0)
// salesTaxCents Int @default(0)
// totalCents Int @default(0)
